<% if(errors && errors.indexOf('name')>=0 ){ %>
    <small id="nameHelp" class="form-text text-muted">Nome da Série é necessário!!</small>
<% } %>

<% if(errors && errors.indexOf('name')>=0 ){ %>
    <small id="nameHelp" class="form-text text-muted">Nome da Série é necessário!!</small>
    <% } %>

    <% if(errors){ %>
        Errors
        <% } %>